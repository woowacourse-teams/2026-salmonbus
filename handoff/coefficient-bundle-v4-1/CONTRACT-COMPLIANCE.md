# Contract compliance

Authority order is current dev API/worker at `d856d10819bf1d018ad43fa63714cc348f1fc643`, then the
be-models cross-spec, then observed convention. Site prose is not used to override code.

| v4-1 requirement / field | Current dev consumer contract | Current calculation implementation | Actual source/value | Verdict | Impact | Follow-up for v4-1 |
|---|---|---|---|---|---|---|
| Manifest top level | `BundleManifestReader.java:44-49` allows exactly 19 camelCase fields and rejects unknown fields at line 132 | Emits exactly that set and canonical sorted UTF-8 JSON without final LF | Provisional/final manifest count is checked in Python and Java | MATCH | Loader can parse the envelope | Keep strict Python and exact Java checks |
| Schema/model identity | `BundleLoader.java:32-36,64-83` requires `a18-live-bundle-v1`, `seat-distribution-a18-v1`, routes `[1650,3330]`, horizons 1..12 | Emits exact constants and axis order | S3 and d856 live RDS now contain both routes | MATCH | Both required tensor axes have live route versions | Keep route-axis order fixed |
| 31 feature names/order | `SeatForecastDesignMatrix.java:123-154`; loader compares exact list at `BundleLoader.java:83` | Uses the exact list/order in `v41_model.py` | Both route blocks use 31 columns | MATCH | Prevents silent column permutation | Java feature fixture and core tests remain release gate |
| Constant-zero features | Current Java leaves one-based columns 4, 19 and 21..28 zero | Materializer sets them zero; hurdle/sign/bin coefficients are forced to raw-bit `+0.0` | Ten axes are structurally unidentifiable | MATCH | No invented time-slot/route/spline meaning | Validator checks every feature-bearing tensor and unfitted bins |
| Units/constants | Java low-seat width 20 and largest-seat divisor 68 (`SeatForecastDesignMatrix.java:172-175`); stop position is target/largest | Uses seats, seconds, KST hours, ratios and population z exactly | Observed seats fit 0..70 and route maxima are 89/85 | MATCH | Same physical units at train and serve | Keep normalization map exactly two entries |
| Time slot | `ForecastTimeSlot` uses batch response time; morning 07..<09, evening 17..<20 KST | Uses `response_received_at` and same hour boundaries | Source has authoritative UTC/KST timestamp pair | MATCH | No generated/arrival-time drift in columns 2/3 | Manifest pins `observation_batch.response_received_at` |
| Capacity | `JdbcVehicleTrajectoryRepository` uses positive per-vehicle route-version maximum through `(time,batch id)` | Cursor-ordered cumulative maximum; cell generation recomputes maximum through g | 78 private vehicle identities; no identity conflict | MATCH | No future maximum leakage | Boundary continuity uses private exact vehicle ID policy |
| Trajectory | Java reads every batch in an open-left 30-minute window; chain breaks on missing batch/regression/jump | Includes success-empty, semantic failure and quarantined empty break batches | Base has 964 semantic failures; quarantine removes 28 records across full authority | MATCH | Cadence gaps are not mistaken for zero vehicles | Preserve failure batches during any RDS migration |
| Preceding/full streak | Java first-entry ordering, equal-time unknown, closest earlier vehicle; distinct-stop full streak | Same sliding-window rules | Same-route tied response timestamps: zero | MATCH | Removes DB/S3 tie ambiguity for this closure | Re-run tie sensitivity if future closure has any tie |
| Target/roster | Boarding target, raw distance 1..12, no wrap | Uses source `gbis-2026-08-19`, 1650=89/turn44/nonboarding24, 3330=85/turn43/nonboarding7 | Both target versions match order/ID/turn/policy; current valid-from values start 9/2 | MATCH | Historical data is not discarded for current validity | User-approved future migration may extend each version backward with rollback ledger; no duplicate version assumed |
| Station mismatch | Source row must agree with route reference | Whole-record quarantine plus empty structural break | Base 27 records/545 rows; catch-up 1 record/24 rows; intermittent 0.0012%, not treated as route change | MATCH | Prevents false station meanings and false trajectory bridges | Keep reason/count by date in receipt |
| generatedAt label lower bound | Java candidates are strictly after generatedAt; exact timestamp absent from S3 | Continuous invariant filter over fixed G grid using response time proxy | G>=90 cannot retain SETTLED under Java's >90 second gap rule | MATCH | Short-horizon selection loss is measured, not hidden | Chosen G and full sensitivity remain in receipt; telemetry absence limits release claim |
| Label state/order | `ArrivalLabelResolver.java:35,43,62-98`: gap, regression, skip, exact target, 2h wait | Reproduces branch order; only invariant SETTLED enters fitting | Label-only and catch-up observations close final-day labels | MATCH | Missing/skipped/lost never become negatives | Keep label-only watermark and catch-up digests immutable |
| Settlement availability | Current settlement interval is 60 seconds; no hard upper-bound telemetry | Primary S=60; sensitivity 0/60/120/300 | Academy cold-start had no pre-smoke scored history | MATCH | Causal cell inclusion is explicit | Re-evaluate with real lag telemetry before release qualification |
| Cell generation | Current formulas: date-equal aggregation, population z, radius4, as-of revision (`StopDemandAggregator`, `StopDemandStatistics`) | Deterministic 6-hour one-way state, h1 SETTLED only, probabilities excluded | Base plus source-authority catch-up feed aggregate seed | MATCH | No model↔cell fixed point or probability leakage | Runtime needs seed merge/import path before activation |
| Five tensors | `BundleTensor` permits hurdle, anchor, sign, bin, fitted only with derived shapes/dtypes | Emits `[2,12,31]`, `[2,12,2]`, `[2,12,2,31]`, `[2,12,2,9,31]`, `[2,12,2,9]` as F64/F64/F64/F64/U8 | Final weights SHA-256 `5b906da9…a3228`; exact Java loader and independent Python shape/dtype/finite checks PASS | MATCH | No missing or unknown tensor and every fitted flag is binary | Retain `processed/python-bundle-validation.json` and loader-only log with the bundle |
| Fitting math | Cross-spec fixes ridge1 IRLS60/floor1e-9/tol1e-10, L1 anchor40/tol1e-9, direction50/bin10 | Implements those exact values in float64 | All 24 blocks pass the row/date/positive-seat gate at G=60; 240 chronological refits; constant-zero and unfitted-bin raw bits are `+0.0` | MATCH | Final coefficients follow the declared numerical contract without fabricated zero-feature signal | Keep `processed/training-receipt.json`, lag and fit sensitivity receipts |
| Calibration | Java has only its same-day prior shift | Replays Java prior for diagnostics; adds no Platt/isotonic/temperature/intercept/PMF layer | `postprocessingOutsideJava=false` | MATCH | Bundle is self-contained in Java semantics | Keep calibration diagnostic-only |
| Required date count | Cross-spec requires at least 30 ended KST dates and 7+7 calibration/holdout | Uses all available 19 ended partitions and explicit provisional 9+5+5 split | Primary S3 has only 19 ended partitions | DIVERGES | Candidate cannot be called release-qualified even if loader passes | Wait for >=11 more ended dates and rebuild canonical split |
| Full-history source authority | d856 refresh fixes a separate half-open boundary per route | Coefficient dates remain ended 19; route-specific catch-up opens seed-only h1 structural rows; overlap excluded | `FINAL_FREEZE_CLOSED`: 149,193 accepted batches / 2,461,308 observations; two post-disable inventories identical, late catch-up objects 0 | MATCH | No verified pre-target history is arbitrarily dropped and closure cannot drift silently | Seed loader is pinned to `processed/final-s3-freeze-closed.json`; retain closure SHA in final manifest receipt |
| Manifest sourceCommit meaning | Cross-spec says exporter/materializer commit; Java harness requires a full lowercase SHA | Uses authoritative consumer commit and binds uncommitted exporter by detached SHA-256 | Commits are forbidden for this assignment | DIVERGES | Git SHA does not retrieve the exact exporter | Commit scripts only after user approval, then rebuild with that SHA |
| Formal calculation version | Bundle feature version must match current writer and differ from temp | Formal=`observed-max-capacity-v1`; backfill policy has a separate ID | Temp bundle=`seat-feature-contract-v4-1-2026-09-02` | MATCH | Formal reader/writer key aligns without reinterpreting temp version | Enforce atomic cutover because temp carriers can create same-name stats |
| Temp forecast/statistics exclusion | Exact deployment 1 tuple and activation/cutover generation manifest | Training reads frozen S3 only; negative test anti-joins temp forecast and exact contaminated generation tuples | Baseline stats=0; latest smoke snapshot reported 151,523 forecasts across both routes; temp stats/h1 were 0 at an earlier check | MATCH | No smoke quality or carrier lineage enters model/seed | Re-freeze exact set at cutover; no version-wide delete |
| Aggregate seed/runtime | Five-tensor bundle cannot carry cell history | Emits privacy-safe hourly totals and read-only validation/cutover procedure | Academy pre-smoke statistics=0; current backend has no seed merge table/path | DIVERGES | Bundle alone must not be called full serving-state parity | Implement/approve seed merge, official first generation and atomic activation |
| Golden/identity/canonical bytes | Loader verifies weights, golden and 12-line identity digest and runs golden parity | Uses exact pinned Java probe to supply expected values/digests | d856 probe, strict verify, loader-only, feature parity and 96 core tests PASS; identity `9bb1a5ac…3632`, manifest SHA-256 `8a39fbf8…33e4` | MATCH | Formal loader acceptance is proven for the exact delivered bytes | Preserve final manifest bytes and all `processed/java-*.log` files |
