# 2026-09-23 — SAL-133 리뷰 반영

- 기록 기준: 2026-09-23 KST, 비교 기준 `c23fabaf8fe8ba7f7a7d9477d8fdce2db4699951`.
- 요약: 기점·회차지의 반복 출발로 역탐색이 일찍 끝나던 문제를 수정하고 애플리케이션의 좌석 상한을 통일한다.
- 상태: 구현 및 로컬 검증 완료. 커밋 여부는 Git 기록으로 확인하며 운영 배포·운영 DB 변경은 하지 않았다.

## 반복 출발 이전의 같은 편도 잔여석까지 제외

- 대상: `ReverseBoundarySearch`, `TripQualityRepository`, `OneWayTripClassifier`, V16 마이그레이션.
- 변경 전 → 후: 역탐색에서 바로 이전 관측을 편도 시작으로 가정했다. 이제 같은 기점·회차지의 관측을 거슬러 올라가 첫 출발을 찾으며, 이전 편도의 도착 관측은 포함하지 않는다. 새 역탐색 순회는 순차 Stream으로 처리한다.
- 이유: 리뷰에서 지적한 `출발 → 비출발 → 출발`을 재현한 테스트 4건이 수정 전에 실패했다. 이 경우 두 번째 출발에서 조사를 시작하면 첫 출발과 중간 관측의 잔여석은 예측 입력에서 빠지지 않는다. 테스트 입력은 설명용으로 만든 것이며 운영 원본을 재조회한 결과가 아니다.
- 결과: 앞부분을 포함한 해당 편도 관측의 잔여석과 그 관측으로 만든 예보를 이용 대상에서 제외한다. 원본 관측·예보 행은 보존하고 다른 차량·다음 정상 편도는 유지한다. 정방향 판정 규칙은 유지하며 규칙 기록 버전은 v3로 올린다.
- 재시작: 기존 조사 작업 테이블에 nullable `boundary_candidate_observation_id` 한 열을 추가한다. 실제 탐색 위치와 잠정 출발 후보를 따로 저장해 32묶음 경계나 프로세스 재시작 이후에도 조사한다. 기존 V15는 수정하지 않는다.
- 버전 호환: `HistoricalSchema`의 필수 애플리케이션 DB 버전과 대응 테스트를 V16까지 갱신한다. 전체 검증에서 V15만 허용하던 기존 조건 때문에 과거 자료 정리 도구의 초기화가 거부되는 것을 확인했다.
- 부하: 조사 주기·차량 수·32묶음 제한은 그대로다. 기존 관측 PK 조회 한 번으로 탐색 위치와 후보 최대 2행을 읽는다. 정상 수집 경로에 SQL을 추가하지 않는다. 첫 출발까지 확인하므로 반복 상태가 길면 조사 횟수는 늘 수 있다.

## 좌석 상한의 애플리케이션 기준 통일

- 대상: `SeatGrid`, `OneWayTripClassifier`, `TripQualityRepository`, `JdbcVehicleTrajectoryRepository`, `TripQualityMaintenance`.
- 변경 전 → 후: 모델 외부의 이상 감지·편도 판정·capacity 조회·과거 자료 표본 및 이상 조회에 직접 적힌 70을 `SeatGrid.LARGEST_SEATS`로 교체한다. SQL에는 이 값을 매개변수로 전달한다.
- 이유: 같은 모델 범위를 검사하는 Java 코드들이 서로 다른 기준으로 바뀌는 것을 막는다.
- 결과: 실제 상한은 70석 그대로다. DB 뷰의 70은 별도 기준으로 유지하고, 통합 테스트에서 모델 상한은 허용하고 상한+1은 제외하는지 비교한다. 모델 계약 식별자·학습 계수·원본 관측은 바꾸지 않으며 SQL 실행 횟수도 늘지 않는다.

## 검증 및 남은 작업

- 검증: 전체 Gradle `build` 성공(1분 4초). 최종 테스트 결과 총 1,158건 성공, 실패·오류·스킵 0건(API 193, common 43, migration-tool 68, worker 854). 병렬 작업·테스트 fork 수는 1이며 DB 테스트는 로컬 Testcontainers PostgreSQL이다. 최종 실행에서 변경 없는 common 테스트는 직전 전체 실행의 성공 결과를 재사용했다.
- 관련 검증: `TripQualityMaintenanceTest` 20건, `ReverseBoundarySearchTest` 11건, `OneWayTripClassifierTest` 31건, 마이그레이션 관련 6건 성공. DB 상한과 모델 상한 일치 및 정상 관측의 추가 SQL 0회를 포함한다.
- 확인: 기점·회차지와 비출발 상태 0·1 조합, 이전 편도 도착 보존, 32묶음과 총 10분을 넘는 반복 상태, 트랜잭션 롤백, 새 저장소 인스턴스에서 커밋된 조사 이어가기, 10분/10분 1초 경계, 기존 예보 차단과 원본 보존을 검증했다. 뒤로 조사하는 한 번의 SQL 실행 수는 기존과 같은 8회다.
- 미검증: 운영 데이터 전체의 판정 정확도, 운영 RDS 실행 계획·부하, 배포 및 운영 복구.
- 미구현: 이미 완료된 과거 조사를 자동으로 재등록하지 않는다. 잘못 분류된 과거 자료가 있다면 대상 확인 후 별도 재조사가 필요하다. 관측 자체가 빠진 운행을 완전히 복원하지는 못한다.
