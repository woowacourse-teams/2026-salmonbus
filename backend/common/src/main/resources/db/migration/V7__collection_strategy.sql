-- 어느 주기표로 부른 판인가.
--
-- 간격이 시각대마다 다르고 그 표가 바뀌면 관측 밀도가 통째로 달라진다. 학습이 밀도 위에 서 있어서
-- (연속 관측 간격 90초 이하라는 라벨 조건, 순번 점프 2 이상이 0건인 15~20초 구간) 나중에
-- 어느 밀도로 모은 관측인지 못 되짚으면 모델 성적을 비교할 수 없다.
--
-- V6 은 SAL-94 가 다른 가지에서 썼다. 이 가지에는 아직 없다.
--
-- 관측 묶음에 행이 하나도 없다는 전제로 NOT NULL 을 기본값 없이 붙인다.
-- 수집기를 부르는 곳이 여태 없어서 판이 쌓인 적이 없다.
ALTER TABLE observation_batch
    ADD COLUMN collection_strategy_version varchar(40) NOT NULL;
